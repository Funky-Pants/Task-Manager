export default class TaskModel {
    constructor(id, title, descr, state, reating, authorID) {
      this.id = id;
      this.title = title;
      this.description = descr;
      this.state = state;
      this.rating = reating;
      this.authorId = authorID ;
    }
  }
  